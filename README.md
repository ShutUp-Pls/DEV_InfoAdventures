## Dependencias del sistema

Este proyecto requiere SDL2 y pkg-config:

### Ubuntu / Debian
sudo apt install libsdl2-dev pkg-config

### Arch / Manjaro
sudo pacman -S sdl2 pkgconf

### Fedora
sudo dnf install SDL2-devel pkgconf
